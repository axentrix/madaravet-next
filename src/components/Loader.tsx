"use client";

// Loader is controlled by custom.js - this component doesn't do anything
// The loader animation is handled at the end of loadTranslations() in custom.js
export default function Loader() {
  return null;
}
