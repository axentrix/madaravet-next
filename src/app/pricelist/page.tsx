import PricelistClient from '@/components/PricelistClient';

export default function PricelistPage() {
  return (
    <div id="pricelist-page" className="page-pricelist">
      <PricelistClient />
    </div>
  );
}
